import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class AdminService {
  // Admin-related API calls and business logic
  // Examples: getUsers(), getMovie(), updateMovie(), deleteMovie()
}
