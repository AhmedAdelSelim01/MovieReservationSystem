import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class MoviesService {
  // Movie-related API calls and business logic
  // Examples: getMovies(), getMovieDetails(), searchMovies()
}
