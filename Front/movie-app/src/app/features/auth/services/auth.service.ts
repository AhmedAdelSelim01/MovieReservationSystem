import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class AuthService {
  // Authentication service methods will go here
  // Examples: login(), register(), logout(), getCurrentUser()
}
